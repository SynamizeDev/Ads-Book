services:
  - type: web
    name: adsbook-backend
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "node index.js"
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5000
